import './styles.css'
import { useEffect } from 'react'

function Edit(){


    // get data when component mounts
    // api/reviews/:id
    // storedata in state
    //create form the same as the property name of the data - make sure name is same as schema
    // form value will be controlled by the state the value of setWhaterver
    // handle change function 

    useEffect(() =>{
        
    })
    return(
        <>
        <br />
        <br />
        <br />
        <h1>Test</h1>
        </>
    )
}

export default Edit